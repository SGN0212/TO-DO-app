---
description: Especificaciones para uso de ViewBinding en layouts Android
rule_type: auto
globs:
  - "ui/**/*.kt"
---
- Always bind your views using ViewBinding: inflate binding in `onCreateView()` or `onCreate()`.
- Do not use `findViewById`.
- Use `binding.apply { ... }` or `with(binding)` when interacting con múltiples vistas.
- Cierra `binding = null` en `onDestroyView()` dentro de Fragments.